package org.infosec.ismp.manager.winsensor;

/**
 * @author Rocky
 * @version create time: Dec 28, 2010 3:37:58 PM
 *
 */
public class Constants {

	/*
	 * Hibernate 批量更新大小
	 */
	public final static int HIBERNATE_JDBC_BATCH_SIZE = 50;
	
	/*
	 * WindowsLog批量保存中，当距离上次保存时间超过设定间隔时间时则触发保存动作。
	 */
	public final static int WINDOWS_LOG_BATCH_SAVE_INTERVAL = 2 * 60 * 1000;
	
	/*
	 * WindowsLog批量保存中，当缓存超过设定值时则触发保存动作。
	 */
	public final static int WINDOWS_LOG_BATCH_SAVE_MEMORY_SIZE = 100;
	
	/*
	 * HostResourceHistory批量保存中，当距离上次保存时间超过设定间隔时间时则触发保存动作。
	 */
	public final static int HOST_RESOURCE_HISTORY_BATCH_SAVE_INTERVAL = 1 * 60 * 1000;
	
	/*
	 * HostResourceHistory批量保存中，当缓存超过设定值时则触发保存动作。
	 */
	public final static int HOST_RESOURCE_HISTORY_BATCH_SAVE_MEMORY_SIZE = 500;
}	
