package edu.sjtu.infosec.ismp.manager.EM.web;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;

import org.springframework.beans.factory.BeanFactory;
import org.springframework.web.context.support.WebApplicationContextUtils;

//import edu.sjtu.infosec.ismp.manager.server.event.process.EventTopoStatistics;

public class PipeServlet extends HttpServlet{

	@Override
	public void init() throws ServletException {
		BeanFactory bf = WebApplicationContextUtils.getRequiredWebApplicationContext(this.getServletContext());
		//EventReceiveImpl eventReceiveImpl = (EventReceiveImpl)bf.getBean("eventReceiveImpl");
		//eventReceiveImpl.main(null);
		super.init();
	}

	
}
