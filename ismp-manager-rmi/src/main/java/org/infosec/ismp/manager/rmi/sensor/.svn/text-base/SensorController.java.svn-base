package org.infosec.ismp.manager.rmi.sensor;

import java.util.Map;

/**
 * @author Rocky
 * @version create time：Nov 9, 2010 4:32:38 PM
 * 日志审计中Sensor日志的管理
 */
public interface SensorController {
	/**
	 * 添加日志源
	 * @param sensorMap key：sensorId（Sensor唯一标识）；value：日志上报周期
	 */
	public void addSensor(Map<String, Long> sensorMap);
	
	/**
	 * 删除日志源
	 * @param sensorId Sensor唯一标识
	 */
	public void deleteSensor(String sensorId);
}
