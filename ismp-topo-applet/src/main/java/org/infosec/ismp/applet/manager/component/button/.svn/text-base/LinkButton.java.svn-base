package org.infosec.ismp.applet.manager.component.button;

import org.infosec.ismp.applet.manager.model.LinkModel;

import twaver.TUIManager;
import twaver.network.TNetwork;
import twaver.network.toolbar.NetworkCreateLinkButton;

/**
 * 连接线按钮
 * @author 肖高峰
 */

@SuppressWarnings("serial")
public class LinkButton extends NetworkCreateLinkButton{
	public LinkButton(TNetwork network) {
		super(network, LinkModel.class);
		this.setIcon(TUIManager.getIcon(LinkModel.class));
		
	}
}
